load("@pybind11_bazel//:build_defs.bzl", "pybind_extension")

pybind_extension(
    name = "mango_iv",
    srcs = ["mango_bindings.cpp"],
    deps = [
        "//src/pricing:iv_solver",
        "//src/pricing:american_option",
        "//src/core:root_finding",
        "//src/pricing:iv_types",
        "//src/utils:expected",
    ],
    copts = [
        "-std=c++20",
        "-Wall",
        "-Wextra",
        "-O3",
        "-fopenmp",
        "-DHAVE_SYSTEMTAP_SDT",
    ],
    linkopts = ["-fopenmp"],
    visibility = ["//visibility:public"],
)
